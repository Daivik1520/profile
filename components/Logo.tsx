export default function Logo() {
  return (
    <span
      style={{
        fontFamily: "'Manuka', sans-serif",
        fontSize: "1.6rem",
        fontWeight: 900,
        color: "#FFD177",
        letterSpacing: "0.02em",
        lineHeight: 1,
        marginTop: "0.15em",
      }}
    >
      DR
    </span>
  );
}
