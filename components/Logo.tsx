export default function Logo() {
  return (
    <span
      style={{
        fontFamily: "'Manuka', sans-serif",
        fontSize: "0.9rem",
        fontWeight: 900,
        color: "#FFD177",
        letterSpacing: "0.02em",
      }}
    >
      DR
    </span>
  );
}
